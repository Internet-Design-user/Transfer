# Transfer
基于vue2+elementui实现带有虚拟滚动的穿梭狂

## BaseDialog是el-dialog的二次封装，无需关心，可直接去掉
## function cryptoNum() { // 自行下载crypto，该方法返回的是个随机数
    const buf = crypto.randomBytes(12)
    const num = buf.toString('hex')
    return num
}
## 可能一些样式需要自行填充
